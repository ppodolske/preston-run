function optionalText(value){const text=String(value??'').trim();return text||null;}
function payload(input={}){return {title:String(input.title??'').trim(),status:input.status||'open',due_at:input.due_at||null,priority:input.priority||'normal',linked_life_item_id:optionalText(input.linked_life_item_id),linked_person_id:optionalText(input.linked_person_id),linked_trip_id:optionalText(input.linked_trip_id),notes:optionalText(input.notes)};}
async function listTasks(supabase){const result=await supabase.from('tasks').select('*').order('due_at',{ascending:true,nullsFirst:false}).order('title',{ascending:true});if(result.error)throw result.error;return result.data||[];}
async function getTask(supabase,id){const result=await supabase.from('tasks').select('*').eq('id',id).maybeSingle();if(result.error)throw result.error;return result.data||null;}
async function createTask(supabase,user,input){if(!user||!user.id)throw new Error('Authenticated user is required');const result=await supabase.from('tasks').insert({...payload(input),user_id:user.id}).select('*').single();if(result.error)throw result.error;return result.data;}
async function updateTask(supabase,user,id,input){if(!user||!user.id)throw new Error('Authenticated user is required');const result=await supabase.from('tasks').update({...payload(input),updated_at:new Date().toISOString()}).eq('id',id).eq('user_id',user.id).select('*').maybeSingle();if(result.error)throw result.error;return result.data||null;}
async function deleteTask(supabase,user,id){if(!user||!user.id)throw new Error('Authenticated user is required');const result=await supabase.from('tasks').delete().eq('id',id).eq('user_id',user.id).select('id').maybeSingle();if(result.error)throw result.error;return Boolean(result.data);}
module.exports={listTasks,getTask,createTask,updateTask,deleteTask,payload};
